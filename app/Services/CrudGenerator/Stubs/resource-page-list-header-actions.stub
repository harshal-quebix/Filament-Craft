
    protected function getHeaderActions(): array
    {
        $actions = [];

        if (auth()->user()->can('create {{modelWords}}')) {
            $actions[] = CreateAction::make()
                ->label(__('Add {{modelName}}'))
                ->tooltip(__('Create {{modelName}}'))
                ->icon('heroicon-o-plus');
        }

        return $actions;
    }
